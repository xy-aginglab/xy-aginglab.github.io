---
layout: bloom
---
<div class="bloom-page-header">
  <h1>{{ page.title }}</h1>
  {% if page.description %}
    <p>{{ page.description }}</p>
  {% endif %}
</div>
<div class="bloom-prose">
  {{ content }}
</div>
